# No dot-directories except github/vscode
.*/
!.vscode/
!.github/

*.code-workspace
node_modules
nbproject

# npm package files
iobroker.*.tgz

Thumbs.db
.commitinfo

# i18n intermediate files
admin/i18n/flat.txt
admin/i18n/*/flat.txt

# Compiled output. Not tracked: CI rebuilds it (build: true on the adapter-tests and
# deploy jobs) and npm packs it from `files`, so the committed copy was pure churn.
build/

# @iobroker/dev-server working directory (local-only, never commit)
.dev-server/

# Local-only dev notes (never commit)
CLAUDE.md
**/CLAUDE.md

# Local review-gate marker
.review/
.DS_Store
